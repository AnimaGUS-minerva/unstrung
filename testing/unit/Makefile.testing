INC=${UNSTRUNG_SRCDIR}/include

# local install of pcap.h, or make sure you have libpcap-dev installed.
CXXFLAGS+=${LIBPCAPINC} -g ${CROSSFLAGS} ${MBEDTLSH}
CFLAGS+=${CROSSFLAGS} ${MBEDTLSH}
LIBS=${LIBFAKEIFACE} ${LIBNDMGMT} ${LIBOSW} ${LIBNETLINK} ${LIBOSW}
LIBS+=${LIBGRASP} ${LIBCBOR} ${LIBCRYPT}
LDFLAGS+=${EXTRALIBS} ${LIBS} ${LIBPCAP}
